rocket:
    default_connection: default
    connection_class:   ~
    connections:
        default:
            class: ~
            params:
                dsn:        'sqlite::memory:'
                username:   ~
                password:   ~
                attributes: ~
