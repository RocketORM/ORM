{% extends 'Column/integer.sql.twig' %}

{% block type -%}
    {% spaceless %}
        {% if null != column.size %}
            float({{ column.size }},{{ column.decimal }})
        {% else %}
            float
        {% endif %}
    {% endspaceless %}
{%- endblock %}