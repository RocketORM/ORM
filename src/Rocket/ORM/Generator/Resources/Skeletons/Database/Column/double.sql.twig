{% extends ['@' ~ driver ~ '/Column/float.sql.twig', 'Column/float.sql.twig'] %}

{% block type -%}
    {% spaceless %}
        {% if null != column.size %}
            double({{ column.size }},{{ column.decimal }})
        {% else %}
            double
        {% endif %}
    {% endspaceless %}
{%- endblock %}