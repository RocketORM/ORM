    /**
     * @param {{ relation.relatedTable.phpName }} ${{ relation.relatedTable.phpName(false) }}
     *
     * @return $this
     */
    public function add{{ relation.relatedTable.phpName }}({{ relation.relatedTable.phpName }} ${{ relation.relatedTable.phpName(false) }})
    {
        $this->{{ relation.phpName(false) }}[] = ${{ relation.relatedTable.phpName(false) }};
    }