    /**
     * @param {{ column.typeAsPhpDoc }} ${{ column.phpName }}
     *
     * @return $this
     */
    public function set{{ column.phpName(true) }}({% block definition %}${{ column.phpName }}{{ not column.isRequired ? ' = null' }}{% endblock %})
    {
        $this->{{ column.phpName }} = ${{ column.phpName }};
        $this->_isModified = true; {#- TODO in the case of this is the primary that change, need to update child relations #}


        return $this;
    }