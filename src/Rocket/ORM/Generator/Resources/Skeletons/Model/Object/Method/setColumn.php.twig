    /**
     * @param {{ column.typeAsPhpDoc }} ${{ column.phpName }}
     *
     * @return $this
     */
    public function set{{ column.methodName }}({% block definition %}${{ column.phpName }}{% endblock %})
    {
        $this->{{ column.phpName }} = ${{ column.phpName }};

        return $this;
    }