    /**
     * @param {{ column.typeAsPhpDoc }} ${{ column.phpName }}
     *
     * @return $this
     */
    public function set{{ column.methodName }}({% block definition %}${{ column.phpName }}{{ not column.isRequired ? ' = null' }}{% endblock %})
    {
        $this->{{ column.phpName }} = ${{ column.phpName }};

        return $this;
    }